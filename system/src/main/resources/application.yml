server:
  port: 9001
  servlet:
    context-path: /system
spring:
  application:
    name: system
  datasource:
    url: jdbc:mysql://localhost:3306/courseimooc?useSSL=false&serverTimezone=UTC
    username: courseimooc
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
mybatis:
  mapper-locations: classpath:/mapper/*.xml
logging:
  level:
    com.ahut.system.mapper: trace